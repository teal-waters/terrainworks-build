 # Input file for BldGrds
  DEM FILE: data/test_dem.tif
  SCRATCH: ./
  CHANNEL MASK: FILE=data/test_channel_mask, RADIUS=10.0, DIRECTIONAL, INIT ALL
  NO CHANNELS: # this precludes initiation of channels other than those specified in the channel mask
  # GRADIENT LENGTH SCALE: 20. # Optional, only if a length scale different than 2 DEM-cell widths is wanted
# OUTPUT NODE POINT SHAPEFILE: node_bldgrds, SPLITS = 1  # Optional, to check that channels are properly enforced
HAND RASTER: hand_1km, AREA THRESHOLD = 1.0 # Area threshold in square kilometers # Optional, outputs .flt
TWI RASTER: twi_10  # Optional, outputs .flt
